% Определение базового случая: сумма пустого списка равна 0
sum_list([], 0).

% Определение рекурсивного случая: сумма списка равна сумме первого элемента
% и суммы оставшейся части списка
sum_list([Head|Tail], Sum) :-
    sum_list(Tail, RestSum), % Рекурсивное вычисление суммы оставшейся части списка
    Sum is Head + RestSum. % Сумма равна первому элементу плюс сумма оставшейся части списка


?- sum_list([1, 2, 3, 4], Sum), write(Sum).
Sum = 10.
